const smash = require('./smash');
const delegate = require('./delegate');
const { unset, unsetEmptyProperties } = require('./unset');

module.exports = {
    smash,
    delegate,
    unset,
    unsetEmptyProperties
};
